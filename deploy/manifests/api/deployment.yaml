apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  namespace: uber
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      imagePullSecrets:
        - name: pullsecret-ghcr
      containers:
        - name: api
          image: ghcr.io/johnmayou/uber/api:sha-2a9784a304626b25ec2fb455d19074fcd24aaaa8
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          ports:
            - containerPort: 8080
